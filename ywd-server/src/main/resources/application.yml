spring:
  application:
    name: Ywd_Server-1
server:
  port: 1111
eureka:
  client:
    prefer-same-zone-eureka: true

    region: shanghai
    availability-zones:
      shanghai: zone-1
    service-url:
      zone-1: http://localhost:1111/eureka/